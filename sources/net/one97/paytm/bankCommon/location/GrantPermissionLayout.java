package net.one97.paytm.bankCommon.location;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.RelativeLayout;
import kotlin.g.b.k;

public final class GrantPermissionLayout extends RelativeLayout {
    /* JADX INFO: super call moved to the top of the method (can break code semantics) */
    public GrantPermissionLayout(Context context) {
        super(context);
        k.c(context, "ctx");
    }

    /* JADX INFO: super call moved to the top of the method (can break code semantics) */
    public GrantPermissionLayout(Context context, AttributeSet attributeSet) {
        super(context, attributeSet);
        k.c(context, "ctx");
        k.c(attributeSet, "attrs");
    }
}
